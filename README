

my $git = Git::Wrapper.new( 
    git     => '/path/to/git',
    gitdir  => '/foo/bar',
);

$git.git;           # path to git binary being used 
$git.version;       # version of git being used
$git.dir;           # path to git repo


